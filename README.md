# Compiler

This is a compiler project.

### Installation

1. Run `./scripts/create_symlinks.sh` to generate symlinks to the compiled binary.
2. Run `make` to compile the code.
3. Run `./compiler` to run the program.


